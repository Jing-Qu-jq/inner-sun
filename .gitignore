# dependencies
node_modules

# build output
dist
build

# secrets — never commit real env files
.env
.env.local
.env.*.local

# editor / OS
.idea/
.DS_Store

# Claude Code: personal machine settings stay local; launch.json is shared dev config
.claude/settings.local.json

# logs
*.log
npm-debug.log*

# TypeScript incremental build cache
*.tsbuildinfo
